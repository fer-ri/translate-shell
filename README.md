# Translate Shell Docker Image

This repository connected to Hub Docker and run automated build.

Base image using Alpine.

### Example

```
docker run --rm ferri/translate-shell \
    -brief \
    'Saluton, Mondo!'
```